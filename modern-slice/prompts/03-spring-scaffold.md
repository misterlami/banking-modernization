
Generate a Spring Boot 3 project in modern-slice/ implementing `openapi/openapi.yaml`.

Requirements:

- Controllers mapped exactly to the spec
- Service layer with TransferService and unit-testable business logic
- Global exception handler that returns the error schema
- Bean validation annotations aligned with the spec
- Provide Maven wrapper and a single command to run tests

Also list the exact files you created/changed.
