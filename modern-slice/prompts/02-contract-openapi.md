
Using the legacy flow findings, produce an OpenAPI 3.0 spec for:

- POST /auth/login
- POST /accounts/{accountId}/transfer

Requirements: JWT bearer auth for transfer, validation rules, consistent error schema, and 2 examples per endpoint (success + failure).

Output only openapi.yaml plus a short explanation of decisions and any assumptions.
