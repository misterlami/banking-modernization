
Create a GitHub Actions pipeline that runs:

1. build and tests
1. dependency vulnerability scan (choose a common tool)
1. fails the build on high severity findings (define threshold)

Output the YAML and how to run similar checks locally.
